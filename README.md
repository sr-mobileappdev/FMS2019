# FMC

Building workflow

1. yarn install or npm install
2. react-native link
3. react-native run-android or run-ios

- iOS build steps:

1. Navigate project folder/ios and run following command
   pod install
2. Open XCode with FMC.xcworkspace and build on XCode
